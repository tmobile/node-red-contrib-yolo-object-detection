{
    "name": "node-red-contrib-yolo-object-detection",
    "version": "1.0.0",
    "description": "",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "postinstall" : "cd model-server && python3 -m venv env && source env/bin/activate && pip3 install -r requirements.txt"
    },
    "author": "",
    "license": "ISC",
    "node-red": {
        "nodes": {
            "lower-case": "yolo-object-detection.js"
        }
    },
    "files": [
        "python_scripts/*"
    ],
    "devDependencies": {
        "eslint": "^7.3.0",
        "eslint-config-standard": "^14.1.1",
        "eslint-plugin-import": "^2.21.2",
        "eslint-plugin-node": "^11.1.0",
        "eslint-plugin-promise": "^4.2.1",
        "eslint-plugin-standard": "^4.0.1"
    }
}
